import 'package:flutter/material.dart';

class AppColors {
  // Primary colors - Merah Putih (Red & White)
  static const Color primary = Color(0xFFC62828); // Merah
  static const Color primaryLight = Color(0xFFEF5350);
  static const Color primaryDark = Color(0xFF7B1FA2);

  // Secondary colors
  static const Color secondary = Color(0xFFFFFFFF); // Putih
  static const Color secondaryLight = Color(0xFFF5F5F5);
  static const Color secondaryDark = Color(0xFFE0E0E0);

  // Background colors
  static const Color background = Color(0xFFFAFAFA);
  static const Color surface = Color(0xFFFFFFFF);

  // Text colors
  static const Color textPrimary = Color(0xFF212121);
  static const Color textSecondary = Color(0xFF757575);
  static const Color textHint = Color(0xFFBDBDBD);

  // Status colors
  static const Color success = Color(0xFF4CAF50);
  static const Color warning = Color(0xFFFFC107);
  static const Color error = Color(0xFFDC3545);
  static const Color info = Color(0xFF2196F3);

  // Doctor types
  static const Color doctorUmum = Color(0xFF4CAF50); // Green
  static const Color doctorPoli = Color(0xFF2196F3); // Blue
  static const Color doctorSpesialis = Color(0xFF9C27B0); // Purple

  // Neutral colors
  static const Color divider = Color(0xFFE0E0E0);
  static const Color border = Color(0xFFD0D0D0);

  // Transparent
  static const Color transparent = Color(0x00000000);
}
